# ERRORS — failure log

Things that did NOT work after two attempts, and what worked instead. Claude
checks this before similar tasks so the same dead ends aren't re-tried.

<!-- Copy this block per failure:

## <short symptom>
**Context:** <when / where it happens>
**Tried:** <what failed, and the error>
**Fix:** <what actually worked>

-->
