# secrets.example.txt — manifest of secret NAMES (never values).
#
# One secret name per line. Anything after '#' is a comment.
# You may add an inline description with 'NAME = human description'.
# Commit THIS file. Never commit a file that contains real values.
#
# Resolution order at runtime: process environment first, then a vault
# CLI (e.g. `infisical secrets get NAME --plain`) if one is configured.

OPENAI_API_KEY      = LLM provider key used by the app
DATABASE_URL        = Postgres connection string
STRIPE_SECRET_KEY   = Payments secret key
GITHUB_TOKEN        = CI / repo automation token
